import styled from "styled-components";
import theme from "utils/theme";

const CreatedAtInfo = styled.span`
  font-weight: ${theme.fontWeight.bold};
  color: grey;
  font-size: ${theme.fontSize.xs};
`;

export default CreatedAtInfo;
